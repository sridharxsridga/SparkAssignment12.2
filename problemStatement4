/**
  Given a list of strings - List[String] ("alpha","gamma","omega","zeta","beta");
  find the count of all strings which start with the alphabet "a"
*/

object problemStatement4{
  def main(args:Array[String]){
    //First declare a list of strings and store it to val variable signs
    val signs = List[String]("alpha","gamma","omega","zeta","beta");

    // find the count of all strings which start with the alphabet "a"
    val countA =signs.count(s => s.startsWith("a"));

    //Print them
    println("Count of the strings which start with the alphabet 'a' : " + countA);
}
}